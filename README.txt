get recipe

DESCRIPTION
	web application that takes user input in the form of ingredients (chicken or beef, salt) and returns 30 options, options are clickable and will link to recipe website

LAUNCH
	open terminal inside directory
	type "npm i"
	type "node server.js" or "npm start"

TESTING
	http://localhost:3000